ARG ARCH_IMAGE=docker.io/archlinux/archlinux:base-20260901.0.583369@sha256:8c609bcd092b9b5024a57b26134c35db6a9d78915b7806b71e6dd709c38d5498

FROM ${ARCH_IMAGE} AS base

ARG VERSION=unknown
ARG REVISION=unknown

LABEL org.opencontainers.image.title="DBB Arch Linux base"
LABEL org.opencontainers.image.description="Snapshot-pinned minimal Arch Linux environment"
LABEL org.opencontainers.image.source="https://github.com/dbent/containers"
LABEL org.opencontainers.image.documentation="https://github.com/dbent/containers#arch-linux"
LABEL org.opencontainers.image.version="${VERSION}"
LABEL org.opencontainers.image.revision="${REVISION}"
LABEL dev.dbent.containers.variant="base"

USER root

RUN printf '%s\n' \
        'Server = https://archive.archlinux.org/repos/2026/09/01/$repo/os/$arch' \
        > /etc/pacman.d/mirrorlist \
    && pacman -Syyuu --noconfirm \
    && printf 'y\ny\n' | pacman -Scc

FROM base AS dev

LABEL org.opencontainers.image.title="DBB Arch Linux development"
LABEL org.opencontainers.image.description="Arch Linux build environment for development and Actions jobs"
LABEL dev.dbent.containers.variant="dev"

RUN pacman -S --needed --noconfirm \
        actionlint \
        base-devel \
        ca-certificates \
        curl \
        git \
        jq \
        just \
        nodejs \
        nushell \
        openssh \
        reuse \
        rumdl \
        tar \
        unzip \
        zip \
        zstd \
    && printf 'y\ny\n' | pacman -Scc
